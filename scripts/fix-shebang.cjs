// Minimal fix-shebang script
// This script is called after build to fix shebang lines in CLI scripts
// For now, just exit successfully

console.log('[fix-shebang] No fix needed (ESM modules)');
process.exit(0);
