# Plan 01-03: Update psypi-commit Tool Definition

## Objective
Update the extension_generator.gleam tool definition for psypi-commit to match the simplified interface (no review_id parameter).

## Tasks

### Task 1: Update commit_tool() in extension_generator.gleam
**Type:** refactor
**Files:** src/extension_generator.gleam
**Action:**
- Remove `review_id` parameter from PiParam list
- Update description from "Commit with Monitor inter-review." to "Commit changes with agent ID tagging. S-bot only."
- Update fn_name if on_commit signature changed
- Update args to match new on_commit(message, ctx, pi) signature

**Verify:** `gleam build` succeeds

### Task 2: Rebuild extension.js
**Type:** verify
**Action:** Run `gleam build` to regenerate extension.js, verify the tool definition is correct
**Verify:** extension.js contains updated psypi-commit tool with single `message` param

**Done when:** psypi-commit tool has only `message` param, no `review_id`

## Deviation Rules
1. Auto-fix bugs — fix immediately, document
2. Auto-add missing critical — add immediately, document
3. Auto-fix blockers — fix immediately, document
4. Ask about architectural — stop and ask user
5. Log enhancements — log to ISSUES.md, continue
