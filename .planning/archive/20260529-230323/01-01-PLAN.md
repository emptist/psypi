# Plan 01-01: Simplify tool_commit.gleam

## Objective
Remove two-phase commit logic. Replace with simple commit that appends agent ID.

## Tasks

### Task 1: Rewrite tool_commit.gleam
**Type:** refactor
**Files:** src/tool_commit.gleam
**Action:**
- Remove `trigger_review()` function (phase 1)
- Remove `commit_if_reviewed()` function (phase 2)
- Remove `on_commit()` two-phase dispatch
- Remove `inter_review` import
- New `on_commit(message, ctx, pi)`:
  1. Get agent ID via `agent_identity.get_current_agent_id(ctx)` or equivalent
  2. Append `[AI:<agent-id>]` to commit message
  3. Run `git add -A && git commit -m "<message> [AI:<agent-id>]"`
  4. Return success/failure
- Keep `shell_escape()` utility

**Verify:** `gleam build` succeeds with no warnings in tool_commit.gleam

**Done when:** tool_commit.gleam has no inter_review import, no two-phase logic, commits with agent ID tag

### Task 2: Verify build
**Type:** verify
**Action:** Run `gleam build` and confirm no compile errors
**Verify:** Build succeeds

## Deviation Rules
1. Auto-fix bugs — fix immediately, document
2. Auto-add missing critical — add immediately, document
3. Auto-fix blockers — fix immediately, document
4. Ask about architectural — stop and ask user
5. Log enhancements — log to ISSUES.md, continue
