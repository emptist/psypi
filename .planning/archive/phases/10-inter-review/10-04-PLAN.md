---
phase: 10-inter-review
type: execute
---

<objective>
Add learning/education to inter-review - Monitor learns from reviews, stores in DB, feeds back to agentbot.

Purpose: Track what agentbots get wrong, store patterns, give education suggestions in review feedback.
Output: Review includes learning suggestions based on patterns.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@docs/INTER_REVIEW_DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design review_learnings table</name>
  <files>src/kernel/db/migrations/</files>
  <action>Create new migration: review_learnings table with fields: id, agentbot_pattern (what went wrong), frequency, suggested_education, last_seen, created_at. This tracks patterns from reviews.</action>
  <verify>ls migration includes new file</verify>
  <done>Table designed</done>
</task>

<task type="auto">
  <name>Task 2: Update psypi-commit to extract patterns from review</name>
  <files>src/psypi/extension_generator.gleam</files>
  <action>After Monitor review, extract patterns from feedback (e.g., "always forget null check", "uses wrong pattern"). Store in review_learnings. Include education suggestions in response.</action>
  <verify>grep "review_learnings" extension.js</verify>
  <done>Pattern extraction implemented</done>
</task>

<task type="auto">
  <name>Task 3: Build and test</name>
  <files>extension.js</files>
  <action>gleam build && regenerate extension.js</action>
  <verify>Build passes</verify>
  <done>Done</done>
</task>

</tasks>

<verification>
- [ ] review_learnings table designed
- [ ] Pattern extraction from Monitor feedback
- [ ] Education suggestions in review response
- [ ] Build passes
</verification>

<success_criteria>
- Monitor learns from each review
- Patterns stored in DB
- Agentbot gets education suggestions in feedback
</success_criteria>

<output>
Create `.planning/phases/10-inter-review/10-04-SUMMARY.md`