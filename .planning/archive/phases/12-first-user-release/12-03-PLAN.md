---
phase: 12-first-user-release
type: execute
---

<objective>
Seed baseline skills from ppi_skills/ database and ensure table_documentation is complete.

Purpose: Fresh DB needs skills in the skills table (not just ppi_skills/ dirs). Table_documentation should cover all core tables so agents can discover the schema.
Output: Skills seeded in DB, table_documentation complete, seed.gleam updated with skills.
</objective>

<context>
@src/skill.gleam
@src/seed.gleam
@ppi_skills/psypi-basics/SKILL.md
@ppi_skills/psypi-dev/SKILL.md
@.planning/phases/12-first-user-release/12-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skills seeding to seed.gleam</name>
  <files>src/seed.gleam</files>
  <action>
    Extend seed.gleam (from Plan 12-01 Task 2) to also seed built-in skills into the skills table.

    Seed these 3 skills if not already present (check by name):

    1. **psypi-basics** — content from ppi_skills/psypi-basics/SKILL.md
       - source: "local", status: "approved", is_enabled: true
       - safety_score: 90
       - description: "Quick cheat-sheet for using Psypi from the TUI (Autonomic vs Somatic, IDs, common tools, commit workflow)"

    2. **psypi-dev** — content from ppi_skills/psypi-dev/SKILL.md
       - source: "local", status: "approved", is_enabled: true
       - safety_score: 85
       - description: (from its frontmatter)

    3. **getting-started** — content from ppi_skills/getting-started/SKILL.md (created in Plan 12-02)
       - source: "local", status: "approved", is_enabled: true
       - safety_score: 95
       - description: "First-time user guide for psypi. Welcome! Start here."

    For each skill:
    - Read the SKILL.md file content
    - Strip the frontmatter (YAML between --- markers) — store as content (jsonb with {markdown: body})
    - Insert into skills table
    - Use INSERT ... ON CONFLICT (name) DO NOTHING for idempotency

    The skills table content column is jsonb. Store as: {"markdown": "<body without frontmatter>"}
  </action>
  <verify>gleam run -m seed — skills appear in DB; SELECT name, status, safety_score FROM skills WHERE source='local';</verify>
  <done>3 local skills seeded in DB, idempotent, seed.gleam handles skill insertion</done>
</task>

<task type="auto">
  <name>Task 2: Complete table_documentation for all core tables</name>
  <files>src/seed.gleam (or direct SQL)</files>
  <action>
    Check which tables are missing from table_documentation and add them. Focus on tables a fresh user/agent would query:

    Current documented: tasks, issues, memory, agent_souls, skills + views, code_versions, system_config, system_reviews, table_documentation, meetings, learning_insights, knowledge_links, mcp_configs, mcp_tools, failure_patterns, failure_alerts, inter_reviews, heartbeat_configs, archived_memory, project_communications

    Missing core tables that should be documented:
    - psypi_config — key-value config store (monitor_debounce_ms, last_wakeup)
    - agent_identities — AI agent identity records
    - agent_sessions — active agent sessions
    - agent_tasks — priorized work items per soul (different from tasks!)
    - agent_prefixes — valid agent ID prefixes (A, S, G)
    - system_directives — autonomic→somatic directives
    - event_hooks — hook registry (already in migration but check docs)

    For each missing table, insert a table_documentation entry with:
    - table_name, purpose (1 line), key_columns (jsonb), related_tables, ai_can_modify=true

    Use SQL directly (psql) for this — it's faster and these are static inserts.

    Note: table already has 9 skill-related entries from our earlier work + 26 existing = ~35 entries. Add ~7 more.
  </action>
  <verify>SELECT count(*) FROM table_documentation; — should be ~42+</verify>
  <done>All core tables documented in table_documentation</done>
</task>

<task type="auto">
  <name>Task 3: Verify MONITOR-DEBOUNCE.md doc accuracy</name>
  <files>docs/MONITOR-DEBOUNCE.md</files>
  <action>
    Update MONITOR-DEBOUNCE.md to be accurate:
    - Correct table name: psypi_config (not system_config)
    - Correct default: 300000ms (5 minutes)
    - Document that the value is read fresh from DB on every agent_end event (no cache)
    - Document that changes take effect immediately (no restart needed)
    - Remove example SQL that uses wrong table name
    - Add note: last_wakeup in psypi_config is used for deduplication

    Current doc has incorrect table reference and wrong examples.
  </action>
  <verify>docs/MONITOR-DEBOUNCE.md has correct table name and accurate examples</verify>
  <done>MONITOR-DEBOUNCE.md is accurate and up to date</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `gleam run -m seed` seeds 3 local skills in DB
- [ ] table_documentation covers all core tables
- [ ] MONITOR-DEBOUNCE.md is accurate
- [ ] All seed operations are idempotent
</verification>

<success_criteria>
- 3 baseline skills seeded in DB (psypi-basics, psypi-dev, getting-started)
- table_documentation complete for all core tables
- MONITOR-DEBOUNCE.md accurate
- All verification checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-first-user-release/12-03-SUMMARY.md`
</output>
