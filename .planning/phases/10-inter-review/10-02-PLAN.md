---
phase: 10-inter-review
type: execute
---

<objective>
Implement intelligent inter-review system that uses Monitor LLM instead of external service.

Purpose: Make inter-review part of the intelligent Monitor system - not a separate external call.
Output: Working psypi-commit that calls Monitor for review.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@docs/INTER_REVIEW_DESIGN.md
@docs/MONITOR_MODES.md
@.planning/phases/10-inter-review/10-01-FINDINGS.md
@src/psypi/extension_generator.gleam
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add psypi-commit tool in extension_generator.gleam</name>
  <files>src/psypi/extension_generator.gleam</files>
  <action>Create psypi_commit_tool() that: 1) gathers git diff, 2) formats context (WHAT + WHY), 3) calls callMonitor(), 4) parses response for PASS/FAIL/score, 5) executes git commit if PASS. Use the existing callMonitor function that uses ctx.model (same model as worker).</action>
  <verify>grep "psypi-commit" src/psypi/extension_generator.gleam returns match</verify>
  <done>Tool function defined in extension_generator.gleam</done>
</task>

<task type="auto">
  <name>Task 2: Build and regenerate extension.js</name>
  <files>extension.js</files>
  <action>Run: rm -rf build/ && gleam build && gleam run -m psypi/extension_generator > extension.js</action>
  <verify>grep "psypi-commit" extension.js returns match</verify>
  <done>Tool appears in extension.js</done>
</task>

<task type="auto">
  <name>Task 3: Remove old commit_tool from inter_review.gleam</name>
  <files>src/psypi/inter_review.gleam</files>
  <action>Remove commit_tool() function from inter_review.gleam - it's now in extension_generator.gleam</action>
  <verify>grep "commit_tool" src/psypi/inter_review.gleam returns no match</verify>
  <done>Old tool removed from inter_review.gleam</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] psypi-commit in extension.js calls callMonitor (uses Monitor LLM)
- [ ] Build passes: gleam build
- [ ] Tool registers in Pi: grep "psypi-commit" extension.js
</verification>

<success_criteria>
- inter_review uses Monitor LLM (not external P-tencent)
- psypi-commit returns PASS/FAIL + score
- git commit executed if review passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-inter-review/10-02-SUMMARY.md`