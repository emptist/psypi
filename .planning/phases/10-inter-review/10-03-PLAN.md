---
phase: 10-inter-review
type: execute
---

<objective>
Add strict Review ID system to inter-review - Monitor generates ID, worker must use it to commit.

Purpose: Create audit trail, ensure each commit has valid recent review, prevent bypassing Monitor.
Output: psypi-commit with --review-id parameter, ID verification, commit logging.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@docs/INTER_REVIEW_DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update psypi-commit to generate REVIEW_ID on review</name>
  <files>src/psypi/extension_generator.gleam</files>
  <action>Modify psypi_commit_tool() to: 1) Always call Monitor for review first, 2) Generate REVIEW_ID (format: REVIEW-{timestamp}-{random}), 3) Return ID + PASS/FAIL + score. If PASS, include ID for next step.</action>
  <verify>grep "REVIEW-" extension.js returns match</verify>
  <done>Tool generates REVIEW_ID on every review</done>
</task>

<task type="auto">
  <name>Task 2: Add --review-id parameter to psypi-commit</name>
  <files>src/psypi/extension_generator.gleam</files>
  <action>Add optional review_id parameter. If provided: 1) Verify ID format, 2) Check ID is recent (within 10 min), 3) Skip re-review, 4) Proceed to commit. This allows commit after getting ID.</action>
  <verify>grep "review_id" extension.js returns match</verify>
  <done>Tool accepts --review-id for commit</done>
</task>

<task type="auto">
  <name>Task 3: Add commit logging with review_id</name>
  <files>src/psypi/extension_generator.gleam</files>
  <action>On successful commit: 1) Log to activity_log with review_id, 2) Log to inter_reviews table (via existing SQL if possible, or just activity_log). Create audit trail.</action>
  <verify>grep "review_id.*log" extension.js returns match</verify>
  <done>Commit includes review ID in logs</done>
</task>

<task type="auto">
  <name>Task 4: Build and verify</name>
  <files>extension.js</files>
  <action>rm -rf build/ && gleam build && gleam run -m psypi/extension_generator > extension.js</action>
  <verify>Build passes, tool in extension.js</verify>
  <done>All changes compile</done>
</task>

</tasks>

<verification>
- [ ] Review generates REVIEW_ID on every call
- [ ] --review-id parameter allows commit
- [ ] ID verification (format + recent)
- [ ] Commit logged with review ID
- [ ] Build passes
</verification>

<success_criteria>
- Strict process: can't commit without valid recent REVIEW_ID
- Audit trail: every commit has review ID logged
- Worker flow: review → get ID → commit with ID
</success_criteria>

<output>
Create `.planning/phases/10-inter-review/10-03-SUMMARY.md`