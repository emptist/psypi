---
phase: 07-gleam-growth
plan: 07-09
type: execute
---

<objective>
FIX the backup disaster & ENSURE SURVIVAL.

Purpose: The backup system saved WRONG versions (incomplete/wrong path wrappers).  
The system APPEARS to work (DISK=DIST correct), but if restored from backup, it would BREAK!  
We must: (1) Fix backup content, (2) Remove covering threat (delete TS files), (3) Verify survival.

Output: Correct wrappers in DB, TS files deleted, backup system verified, system SURVIVES without pnpm.
</objective>

<execution_context>
@/Users/jk/gits/hub/tools_ai/psypi/.pi/skills/create-plans/workflows/execute-phase.md
@/Users/jk/gits/hub/tools_ai/psypi/.pi/skills/create-plans/templates/summary.md
@/Users/jk/gits/hub/tools_ai/psypi/.pi/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@/Users/jk/gits/hub/tools_ai/psypi/.planning/BRIEF.md
@/Users/jk/gits/hub/tools_ai/psypi/.planning/ROADMAP.md
@/Users/jk/gits/hub/tools_ai/psypi/docs/INVESTIGATION-REPORT-2026-05-07.md
@/Users/jk/gits/hub/tools_ai/psypi/docs/DISASTER-REPORT-2026-05-06.md

**The DISASTER (now CLEAR):**
1. Backup has WRONG import path: `../../gleam/...` (SHOULD be `../../../gleam/...`)
2. Wrappers on DISK and DIST are CORRECT (same content)
3. pnpm build is NOT covering anymore (both have correct path now)
4. BUT: If restored from backup → WRONG wrapper → system BREAKS!
5. ROOT CAUSE: 29 .ts.deprecated in src/ + pnpm build can cover wrappers

**User's Original Instruction (from BRIEF.md investigation):**
- "since ts has been saved to database, my origin instruction was to remove them not deprecate them"
- DELETE (not deprecate!) because they're backed up in code_versions!

**Current State:**
- Backup: 1 version with WRONG path
- DISK: CORRECT wrapper
- DIST: CORRECT wrapper  
- TS files: 39 still in src/ (should be DELETED!)
- .ts.deprecated: 29 in src/ (should be GONE!)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Backup - Save CORRECT Wrappers to DB</name>
  <files>All thin JS wrappers in src/kernel/services/*.js</files>
  <action>The backup has WRONG versions (import path `../../gleam/...` instead of `../../../gleam/...`).  
  We must save the CORRECT versions to database:
  
  1. Find all thin JS wrappers: `find src -name "*.js" -not -name "*.ts.deprecated" -not -name "*.mjs.deprecated"`
  2. For each wrapper, read CURRENT DISK version (which is CORRECT)
  3. Save to code_versions with reason: "FIX backup - correct wrapper with proper import path"
  4. Verify: Query DB shows BOTH old (wrong) and new (correct) versions
  
  WHY: If we need to restore, we need WORKING wrappers in DB! The current backup would BREAK the system!
  </action>
  <verify>Run: SELECT file_path, length(content), saved_at::text FROM code_versions WHERE file_path LIKE '%.js' ORDER BY saved_at DESC; - should see new entries with correct path</verify>
  <done>All thin wrappers have CORRECT versions in code_versions database</done>
</task>

<task type="auto">
  <name>Task 2: Delete ALL 39 TS Files from src/</name>
  <files>All .ts files in src/ (39 files total)</files>
  <action>DELETE (not deprecate!) all 39 TypeScript files from src/.  
  User's original instruction: "remove them not deprecate them" because they're in code_versions DB.
  
  Steps:
  1. Verify ALL 39 TS files are in database first (from previous backups)
  2. Delete each .ts file using: git rm or rm (they're in DB!)
  3. Do NOT create .ts.deprecated - just DELETE!
  
  WHY: These files allow pnpm build to potentially cover wrappers. Remove the threat!
  WHY2: They're already backed up in code_versions - no need to keep!
  </action>
  <verify>Run: find src -name "*.ts" ! -name "*.ts.deprecated" | wc -l - should be 0</verify>
  <done>No .ts files remain in src/ (removed covering threat!)</done>
</task>

<task type="auto">
  <name>Task 3: Delete ALL .ts.deprecated and .mjs.deprecated Files</name>
  <files>All .ts.deprecated and .mjs.deprecated in src/, bin/, and project root</files>
  <action>DELETE (rm) all deprecated files from the project:
  
  1. Find all: `find . -name "*.ts.deprecated" -o -name "*.mjs.deprecated"`
  2. DELETE them (rm command)
  3. Also check if deprecated/ folder exists at root - if not, create it
  4. Move any remaining deprecated files to deprecated/ folder at project root
  
  WHY: .ts.deprecated in src/ can confuse tsc (with allowJs: true in tsconfig.json)
  WHY2: Clean up the mess - these files are in DB, no need to keep!
  </action>
  <verify>Run: find . -name "*.ts.deprecated" -o -name "*.mjs.deprecated" | grep -v "deprecated/" | wc -l - should be 0</verify>
  <done>No deprecated files in src/, bin/, or root (only in deprecated/ folder if needed)</done>
</task>

<task type="auto">
  <name>Task 4: Update Docs - DELETE not DEPRECATE!</name>
  <files>.planning/BRIEF.md, AGENTS.md</files>
  <action>Fix the WRONG documentation:
  
  1. BRIEF.md: Change "Never delete - deprecate (use .ts.deprecated extension)" 
     → "DELETE TypeScript files (they're backed up in code_versions database!)"
  
  2. AGENTS.md Rule 0: Change "NEVER DELETE - DEPRECATE ONLY!" 
     → "DELETE (don't deprecate!) - TS files are backed up in code_versions database"
  
  3. Update examples to show: `rm file.ts` (not `mv file.ts file.ts.deprecated`)
  
  WHY: Docs were WRONG! User's original instruction was to REMOVE, not deprecate!
  </action>
  <verify>Run: grep -n "NEVER DELETE\|Never delete" .planning/BRIEF.md AGENTS.md - should return NO matches</verify>
  <done>Docs now say DELETE (backed up in DB!), not deprecate</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Deleted all TS files and deprecated files. Fixed docs. Now need to verify if extension.ts was actually needed by Pi TUI, and if system survives without pnpm.</what-built>
  <how-to-verify>
    1. Check Pi config: cat pi.config.json | grep -A5 "extensions"
    2. Verify extension.ts need: Is it referenced in pi.config.json?
    3. Try running: psypi --help (does Pi TUI load without TS files?)
    4. Verify thin JS wrappers work: head -5 src/kernel/services/AgentIdentityService.js
    5. Confirm gleam build works: cd gleam/psypi_core && gleam build
    6. CRITICAL: Verify backup system works: Make small edit, check DB for new backup
    7. Confirm NO pnpm build needed: grep -r "pnpm build" package.json scripts
  </how-to-verify>
  <resume-signal>Type "approved - extension.ts [was/wasn't] needed, system SURVIVES without pnpm, backup VERIFIED" or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Verify Survival - NO pnpm EVER!</name>
  <files>package.json, tsconfig.json</files>
  <action>Ensure system SURVIVES without pnpm build:
  
  1. Remove build scripts from package.json that use pnpm
  2. Update tsconfig.json: Set "allowJs": false (prevent tsc from processing JS files)
  3. Add reminder to AGENTS.md: "NEVER run pnpm build - only gleam build!"
  4. Verify: Running pnpm build should fail or do nothing
  
  WHY: pnpm build is the ENEMY! It covers wrappers! NEVER use it again!
  WHY2: Only use gleam build (0.04s) for Gleam changes!
  </action>
  <verify>Run: pnpm build 2>&1 | head -5 - should fail or show "nothing to do"</verify>
  <done>pnpm build DISABLED, only gleam build works</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All thin wrappers have CORRECT versions in code_versions (not the wrong-path version!)
- [ ] All 39 TS files DELETED from src/
- [ ] All .ts.deprecated/.mjs.deprecated files DELETED or moved to deprecated/
- [ ] Docs say DELETE not DEPRECATE
- [ ] Pi TUI still functional (extension.ts verified)
- [ ] gleam build works (0.04s)
- [ ] Backup system VERIFIED working (creates new backups on edit)
- [ ] pnpm build DISABLED (system survives without it!)
</verification>

<success_criteria>
- Correct wrappers in code_versions database (import path verified!)
- 0 .ts files in src/ (covering threat removed!)
- 0 .ts.deprecated in src/ (tsc confusion removed!)
- Documentation corrected (DELETE not deprecate!)
- Backup system creates NEW backups with correct content
- System survives with ONLY gleam build (no pnpm ever!)
- Pi TUI functional without TS files
</success_criteria>

<output>
After completion, create `.planning/phases/07-gleam-growth/07-09-SUMMARY.md`:

# Phase 7 Plan 09: Fix & Survive Disaster - Summary

**Accomplishment:** Fixed backup disaster (correct wrappers in DB), deleted all TS files (39), removed covering threat, disabled pnpm build, system now SURVIVES with only gleam build.

## Accomplishments
- Saved CORRECT thin wrappers to code_versions (fixed wrong-path backups)
- Deleted 39 TS files from src/ (User's original instruction: REMOVE not deprecate!)
- Deleted/moved all .ts.deprecated files (removed tsc confusion)
- Fixed docs: Now say "DELETE (backed up in DB)" not "NEVER DELETE"
- Verified backup system works (creates correct backups)
- Disabled pnpm build (system survives with gleam build only)

## Files Deleted
- 39 .ts files from src/ (all now in code_versions DB)
- 29+ .ts.deprecated/.mjs.deprecated files

## Files Modified
- `.planning/BRIEF.md` - Changed to "DELETE not deprecate"
- `AGENTS.md` - Fixed Rule 0
- `package.json` - Removed/disabled pnpm build scripts

## Decisions Made
- extension.ts: [was/wasn't needed - user verified]
- Pi TUI: [works/doesn't work without TS files]

## Issues Encountered
[Problems and resolutions, or "None - smooth execution!"]

## Next Step
Phase 07 complete (all TS removed, system survives). Ready for Phase 5 (Replace Fake Inner AI) or continue Gleam migration.
</output>
