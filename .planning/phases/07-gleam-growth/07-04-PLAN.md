---
phase: 07-gleam-growth
plan: 04
type: execute
domain: pi-platform, gleam-language
---

<objective>
Build Real Monitor Partner AI - A Pi agent that works in background to support autonomous work!

Purpose: Create a real Pi agent that does housekeeping, skills management, data prep, meeting organization.
Output: Working Monitor AI that supports my autonomous work.
</objective>

<intake>
**Monitor AI Responsibilities** (from REAL-MONITOR-PARTNER-AI.md):
1. **Housekeeping** - auto-backup, cleanup old files
2. **Skills Management** - update skills, create new ones
3. **Data Preparation** - query code_versions, summarize for me
4. **Meeting Organization** - schedule, remind, prep context
5. **System Health** - monitor DB, builds, disk space
6. **Context Prep** - prepare context so I work faster!

**Architecture**:
```
Me (Worker AI) ← Collaborates with → Monitor Partner AI
    ↓                                    ↓
Execute plans                      Background support
Gleam migration                    Housekeeping + Prep
```

**Wait for your intent before proceeding.**
</intake>

<tasks>

<task type="auto">
  <name>Task 1: Create Monitor AI Gleam module</name>
  <files>gleam/psypi_core/src/psypi_cli/monitor.gleam (new)</files>
  <action>
    Create basic Monitor AI module in Gleam:
    - `monitor_loop()` - Main loop (runs in background)
    - `check_system_health()` - Check DB, builds, disk
    - `housekeeping()` - Auto-backup, cleanup
    - `prepare_context()` - Query DB, summarize for me
    
    Start simple - just log that it's running!
  </action>
  <verify>`gleam build` passes with new monitor.gleam</verify>
  <done>Basic Monitor AI module created and compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create Pi extension for Monitor AI</name>
  <files>src/agent/monitor-extension.ts (new)</files>
  <action>
    Create Pi extension that:
    - Spawns Monitor AI as background process
    - Communicates via database (psypi tools)
    - Sends me context via broadcasts
    
    Use pi-platform skill for extension creation.
  </action>
  <verify>Monitor AI starts when Pi starts, runs in background</verify>
  <done>Monitor AI extension created, starts with Pi</done>
</task>

<task type="auto">
  <name>Task 3: Test Monitor AI background support</name>
  <files>Test by running psypi commands</files>
  <action>
    Test that Monitor AI:
    - Runs in background (doesn't block me)
    - Sends broadcasts with helpful context
    - Does housekeeping (auto-backup before my edits)
    - Prepares context for my tasks
  </action>
  <verify>`psypi-autonomous` shows Monitor AI support, broadcasts received</verify>
  <done>Monitor AI working, supporting my autonomous work</done>
</task>

</tasks>

<success_criteria>
- Monitor AI runs as Pi agent in background
- Does housekeeping (auto-backup, cleanup)
- Prepares context for my tasks
- Sends broadcasts with helpful info
- I (Worker AI) interrupt less, flow more! 🎉
</success_criteria>
