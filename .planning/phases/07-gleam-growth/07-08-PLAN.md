---
phase: 07-gleam-growth
plan: 07-08
type: execute
---

<objective>
Fix the backup crisis and delete TypeScript files properly (not deprecate!).

Purpose: The investigation revealed that TS files should be DELETED (not .ts.deprecated) because they're already backed up in code_versions database. The current "never delete - deprecate" docs are WRONG and the 29 .ts.deprecated files in src/ are causing pnpm build to potentially overwrite thin JS wrappers.
Output: All 39 TS files deleted from src/, docs fixed, backup system verified working.
</objective>

<execution_context>
@/Users/jk/gits/hub/tools_ai/psypi/.pi/skills/create-plans/workflows/execute-phase.md
@/Users/jk/gits/hub/tools_ai/psypi/.pi/skills/create-plans/templates/summary.md
@/Users/jk/gits/hub/tools_ai/psypi/.pi/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@/Users/jk/gits/hub/tools_ai/psypi/.planning/BRIEF.md
@/Users/jk/gits/hub/tools_ai/psypi/.planning/ROADMAP.md
@/Users/jk/gits/hub/tools_ai/psypi/docs/INVESTIGATION-REPORT-2026-05-07.md
@/Users/jk/gits/hub/tools_ai/psypi/AGENTS.md
@/Users/jk/gits/hub/tools_ai/psypi/.planning/phases/07-gleam-growth/07-01-SUMMARY.md
@/Users/jk/gits/hub/tools_ai/psypi/.planning/phases/07-gleam-growth/07-04-SUMMARY.md

**Key Findings:**
- 39 TS files in src/ (only 10 backed up in code_versions DB)
- 29 .ts.deprecated files STILL in src/ (wrong - should be deleted!)
- Docs say "NEVER DELETE" but user's original instruction was "REMOVE them not deprecate"
- Thin JS wrappers exist but pnpm build might overwrite them
- Backup system last ran at 08:54 (30+ mins ago - may have stopped)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backup 30 Missing TS Files to code_versions</name>
  <files>All 30 TS files NOT in database (see INVESTIGATION-REPORT for list)</files>
  <action>Read each of the 30 TS files that are NOT in code_versions database and use psypi-doc-save to backup each one. Use the GitHub CLI to batch process:
  
  Files to backup (from investigation):
  - src/agent/extension/db.ts
  - src/agent/extension/extension.ts
  - src/agent/index.ts
  - src/agent/monitor-extension.ts
  - src/cli-wrapper.ts
  - src/kernel/config/Config.ts
  - src/kernel/config/constants.ts
  - src/kernel/config/types.ts
  - src/kernel/config/YamlConfigLoader.ts
  - src/kernel/core/ConversationLogger.ts
  - src/kernel/core/EventBus.ts
  - src/kernel/core/KnowledgeGraph.ts
  - src/kernel/core/LearningAnalysis.ts
  - src/kernel/core/LearningRecorder.ts
  - src/kernel/core/PluginManager.ts
  - src/kernel/db/DatabaseClient.ts
  - src/kernel/db/index.ts
  - src/kernel/db/types.ts
  - src/kernel/index.ts
  - src/kernel/services/embedding/index.ts
  - src/kernel/services/embedding/OllamaEmbedding.ts
  - src/kernel/services/embedding/OpenAIEmbedding.ts
  - src/kernel/services/embedding/types.ts
  - src/kernel/services/embedding/ZhipuEmbedding.ts
  - src/kernel/utils/CronParser.ts
  - src/kernel/utils/EnhancedCircuitBreaker.ts
  - src/kernel/utils/cli.ts
  - src/kernel/utils/ErrorClassifier.ts
  - src/kernel/utils/logger.ts
  - src/kernel/utils/ResponseCache.ts
  
  For each file, read content and save to DB. Reason: "backup before deletion - part of 07-08 plan"
  
  WHY: These files are NOT in database. If deleted now, they'd be LOST FOREVER!
  </action>
  <verify>Run SQL: SELECT COUNT(DISTINCT file_path) FROM code_versions WHERE file_path LIKE '%.ts'; - should now be 39+ (was 10 before)</verify>
  <done>All 30 missing TS files are now in code_versions database</done>
</task>

<task type="auto">
  <name>Task 2: Delete ALL 39 TS Files from src/</name>
  <files>All .ts files in src/ (39 files total)</files>
  <action>DELETE (not rename to .ts.deprecated!) all 39 TypeScript files from src/. The user's original instruction was "remove them not deprecate them" because they're already in code_versions database.
  
  Steps:
  1. Verify ALL 39 files are in database first (from Task 1)
  2. Delete each .ts file using rm command
  3. Do NOT use .ts.deprecated - just DELETE them!
  4. This includes extension.ts (we'll verify if it's needed later)
  
  WHY: Files are backed up in DB! No need to keep .ts.deprecated files that confuse tsc and cause pnpm build to potentially overwrite wrappers!
  </action>
  <verify>Run: find src -name "*.ts" ! -name "*.ts.deprecated" | wc -l - should be 0</verify>
  <done>No .ts files remain in src/ (except possibly .ts.deprecated which we'll handle next)</done>
</task>

<task type="auto">
  <name>Task 3: Delete ALL .ts.deprecated and .mjs.deprecated Files from src/</name>
  <files>All .ts.deprecated and .mjs.deprecated files in src/ and bin/</files>
  <action>DELETE (rm) all 29 .ts.deprecated and .mjs.deprecated files from src/ and bin/. These should have been moved to deprecated/ folder at project root, but instead they're clogging up src/ and confusing tsc.
  
  Also move any remaining deprecated files to a proper deprecated/ folder at project root (if not already there).
  
  WHY: .ts.deprecated files in src/ can still be processed by tsc (with allowJs: true). They need to be GONE from src/!
  </action>
  <verify>Run: find src -name "*.ts.deprecated" -o -name "*.mjs.deprecated" | wc -l - should be 0</verify>
  <done>No .ts.deprecated or .mjs.deprecated files remain in src/ or bin/</done>
</task>

<task type="auto">
  <name>Task 4: Fix Docs - Change "NEVER DELETE" to "DELETE"</name>
  <files>.planning/BRIEF.md, AGENTS.md</files>
  <action>Update documentation to reflect CORRECT instruction (DELETE, not DEPRECATE!):
  
  1. In BRIEF.md: Change "Never delete - deprecate (use .ts.deprecated extension)" to:
     "DELETE TypeScript files (they're backed up in code_versions database!)"
  
  2. In AGENTS.md Rule 0: Change "NEVER DELETE - DEPRECATE ONLY!" to:
     "DELETE (don't deprecate!) - TS files are backed up in code_versions database"
     Also update the example to show: mv file.ts /tmp/ (just delete it!)
  
  WHY: The docs were WRONG! User's original instruction was to REMOVE, not deprecate. The database backup makes keeping files unnecessary!
  </action>
  <verify>Run: grep -n "NEVER DELETE\|Never delete" .planning/BRIEF.md AGENTS.md - should return NO matches</verify>
  <done>Docs now say DELETE (not deprecate!) with correct reasoning</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Deleted all 39 TS files and 29 deprecated files. Fixed docs. Now need to verify if extension.ts was actually needed by Pi TUI.</what-built>
  <how-to-verify>
    1. Check if Pi TUI can load without extension.ts:
       - Look at pi.config.json to see how extension is loaded
       - Check if extension.ts is referenced anywhere in Pi config
    2. Try running: psypi --help (does it work without TS files?)
    3. Verify thin JS wrappers work: head -5 src/kernel/services/AgentIdentityService.js
    4. Confirm gleam build still works: cd gleam/psypi_core && gleam build
    5. Check if pnpm build would now work (it shouldn't be needed anymore!)
  </how-to-verify>
  <resume-signal>Type "approved - extension.ts was/wasn't needed, proceed to summary" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 39 TS files deleted from src/
- [ ] All .ts.deprecated files deleted from src/
- [ ] All 39 files now exist in code_versions database
- [ ] BRIEF.md says "DELETE" not "deprecate"
- [ ] AGENTS.md says "DELETE" not "NEVER DELETE"
- [ ] gleam build still works (0.04s)
- [ ] Pi TUI can still load (extension.ts verified)
</verification>

<success_criteria>
- 0 .ts files remain in src/
- 0 .ts.deprecated files remain in src/
- 39+ TS files now in code_versions database
- Documentation corrected (DELETE not deprecate)
- Pi TUI still functional
- No more pnpm build needed (only gleam build)
</success_criteria>

<output>
After completion, create `.planning/phases/07-gleam-growth/07-08-SUMMARY.md`:

# Phase 7 Plan 08: Fix Backup Crisis & Delete TS Files - Summary

**Accomplishment:** Deleted all 39 TS files and 29 deprecated files, fixed docs to say DELETE (not deprecate), verified all files backed up in code_versions database.

## Files Deleted
- 39 .ts files from src/ (all now in DB)
- 29 .ts.deprecated/.mjs.deprecated files from src/

## Files Modified
- `.planning/BRIEF.md` - Changed "Never delete" to "DELETE (backed up in DB)!"
- `AGENTS.md` - Changed Rule 0 to "DELETE not deprecate!"

## Decisions Made
- Extension.ts: [was/wasn't needed - user verified]
- Pi TUI loading: [works/doesn't work without TS files]

## Issues Encountered
- [List any problems and resolutions, or "None"]

## Next Step
Ready for Phase 07-09 (migrate more services to Gleam) or Phase 5 (Replace Fake Inner AI)
</output>
