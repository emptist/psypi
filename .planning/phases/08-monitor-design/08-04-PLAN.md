---
phase: 08-monitor-design
type: execute
---

<objective>
Implement safety hook - tool_call can block dangerous operations.

Purpose: Prevent worker from accidentally running destructive commands (spawn, delete, exec).
Output: tool_call hook that checks and blocks dangerous ops
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@docs/archive/PI_EVENTS_REFERENCE.md
@gleam/psypi_core/src/psypi_cli/extension_generator.gleam
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define dangerous operations list</name>
  <files>gleam/psypi_core/src/psypi_cli/extension_generator.gleam</files>
  <action>Define what operations are "dangerous" in the tool_call hook. Based on Pi docs, tool_call can return {block: true} to stop execution. Dangerous: spawn('pi'), spawn('psypi'), exec('rm -rf'), any command containing 'git push --force', 'DROP TABLE', etc.</action>
  <verify>List documented in comments or constants</verify>
  <done>Clear list of dangerous patterns defined</done>
</task>

<task type="auto">
  <name>Task 2: Implement block logic in tool_call hook</name>
  <files>gleam/psypi_core/src/psypi_cli/extension_generator.gleam</files>
  <action>In the tool_call event hook, check if tool name or arguments match dangerous patterns. If match, return {block: true, message: "Operation blocked by Monitor"} to prevent execution. DO NOT block all tool calls - only dangerous ones.</action>
  <verify>grep "block.*true" extension.js</verify>
  <done>Blocking logic implemented in hook</done>
</task>

<task type="auto">
  <name>Task 3: Build and test blocking</name>
  <files>gleam/psypi_core/src/psypi_cli/extension_generator.gleam</files>
  <action>Run: cd gleam/psypi_core && rm -rf build/ && gleam build && gleam run -m psypi_cli/extension_generator. Then test by calling a dangerous tool in Pi - should be blocked.</action>
  <verify>extension.js contains block logic</verify>
  <done>Dangerous operations blocked in Pi</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Dangerous operations list defined
- [ ] Block logic in tool_call hook
- [ ] Test dangerous tool call → blocked response
</verification>

<success_criteria>
- Dangerous ops list documented
- tool_call hook can block
- No false positives on normal tools
</success_criteria>

<output>
After completion, create `.planning/phases/08-monitor-design/08-04-SUMMARY.md`
</output>