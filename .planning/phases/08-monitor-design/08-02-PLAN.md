---
phase: 08-monitor-design
type: execute
---

<objective>
Implement psypi-monitor-consult tool - allow worker to call Monitor (local LLM) for difficult decisions.

Purpose: Worker can consult Monitor via tool call, gets LLM response with guidance.
Output: Working psypi-monitor-consult tool in Pi.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@docs/archive/PI_EVENTS_REFERENCE.md
@.pi/skills/monitor/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Find existing Pi tool pattern in Gleam</name>
  <files>gleam/psypi_core/src/psypi_cli/</files>
  <action>Look at existing Pi tools (e.g., my_id_tool, task_add_tool) in extension_generator.gleam to understand how PiToolCall is defined and how execute function works. Find the pattern for adding a new tool.</action>
  <verify>Read 3 existing tool definitions, identify: PiToolCall type structure, execute function signature, parameter handling</verify>
  <done>Know how to define a new Pi tool with execute function</done>
</task>

<task type="auto">
  <name>Task 2: Create monitor_consult_tool with LLM call</name>
  <files>gleam/psypi_core/src/psypi_cli/extension_generator.gleam</files>
  <action>Add new PiToolCall for monitor_consult_tool. The execute function should use ctx.model to call LLM with the worker's question. Format: receive question string, call ctx.model.generate() with prompt, return response. DO NOT use spawn() - ctx.model is available in execute.</action>
  <verify>grep shows new tool in extension_generator.gleam</verify>
  <done>New tool defined with execute function that calls LLM</done>
</task>

<task type="auto">
  <name>Task 3: Add monitor_consult_tool to all_tools()</name>
  <files>gleam/psypi_core/src/psypi_cli/extension_generator.gleam</files>
  <action>Import the new tool and add it to all_tools() list. Then run: cd gleam/psypi_core && rm -rf build/ && gleam build && gleam run -m psypi_cli/extension_generator</action>
  <verify>extension.js contains psypi-monitor-consult tool</verify>
  <done>Tool appears in Pi when running psypi</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] grep "psypi-monitor-consult" extension.js returns match
- [ ] gleam build passes
- [ ] Tool responds to "Consult Monitor about..." question
</verification>

<success_criteria>
- monitor_consult_tool defined in Gleam
- Tool appears in extension.js
- LLM call works via ctx.model
</success_criteria>

<output>
After completion, create `.planning/phases/08-monitor-design/08-02-SUMMARY.md`
</output>