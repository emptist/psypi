---
phase: 08-monitor-design
type: execute
---

<objective>
Add before_agent_start event hook to inject Monitor guidance into agent context.

Purpose: When agent starts, Monitor can provide guidance based on recent activity/memories.
Output: before_agent_start hook in extension.js
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@docs/archive/PI_EVENTS_REFERENCE.md
@gleam/psypi_core/src/psypi_cli/extension_generator.gleam
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add before_agent_start to event hooks</name>
  <files>gleam/psypi_core/src/psypi_cli/extension_generator.gleam</files>
  <action>Find where event hooks are defined (all_event_hooks function or similar). Add before_agent_start hook that logs agent start and optionally queries recent memories. The hook should use ctx to access session data.</action>
  <verify>grep "before_agent_start" extension_generator.gleam</verify>
  <done>Hook definition added to generator</done>
</task>

<task type="auto">
  <name>Task 2: Build and regenerate extension.js</name>
  <files>gleam/psypi_core/src/psypi_cli/extension_generator.gleam</files>
  <action>Run: cd gleam/psypi_core && rm -rf build/ && gleam build && gleam run -m psypi_cli/extension_generator</action>
  <verify>grep "before_agent_start" extension.js returns match</verify>
  <done>Extension regenerated with new hook</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] grep "before_agent_start" extension.js returns match
- [ ] Hook fires when agent starts in Pi
</verification>

<success_criteria>
- before_agent_start hook in extension.js
- Hook executes when agent starts
</success_criteria>

<output>
After completion, create `.planning/phases/08-monitor-design/08-03-SUMMARY.md`
</output>