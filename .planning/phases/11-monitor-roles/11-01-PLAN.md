# Phase 11 Plan: Monitor Roles Expansion

## Objective
Expand Monitor system with two new capabilities: statistics tracking (model quality) and self-design (find own jobs).

## Context
@docs/INTER_REVIEW_DESIGN.md - Inter-review system already implemented
@docs/MONITOR_MODES.md - Monitor 3 modes documented
@src/psypi/extension_generator.gleam - Contains Monitor tools (callMonitor, psypi-autonomic-consult)
@src/kernel/db/migrations/057_table_documentation.sql - Shows existing learning_insights table

## Tasks

### Task 1: Statistics - Model Quality Tracking
<type>feature</type>
<files>gleam/psypi_core/src/psypi/monitor/statistics.gleam (new)</files>
<action>
Create Gleam module for tracking model quality:
- Review scores (from inter_review scores)
- Response times
- Error rates

Add SQL function to store statistics in existing tables or create minimal tracking.
</action>
<verify>grep "statistics" gleam/psypi_core/src/psypi/monitor/ returns match</verify>
<done>Statistics module created with basic tracking functions</done>

### Task 2: Self-Design - Monitor Finds Own Jobs
<type>feature</type>
<files>gleam/psypi_core/src/psypi/monitor/self_design.gleam (new)</files>
<action>
Create Gleam module for Monitor to find work:
- Detect when worker is idle
- Propose useful tasks (code improvements, documentation, etc.)
- Emit suggestions via existing communication channels

Focus on lightweight detection - don't add periodic polling.
</action>
<verify>grep "self_design" gleam/psypi_core/src/psypi/monitor/ returns match</verify>
<done>Self-design module created with idle detection</done>

### Task 3: Integration - Hook into existing Monitor flow
<type>integration</type>
<files>gleam/psypi_core/src/psypi/monitor.gleam</files>
<action>
Integrate statistics and self-design into Monitor's existing tool calls:
- Add statistics calls after inter_review completion
- Add self-design check in psypi-autonomic-consult path
</action>
<verify>gleam build succeeds after integration</verify>
<done>Statistics and self-design integrated into Monitor flow</done>

## Verification
- [ ] gleam build succeeds
- [ ] Extension.js regenerates correctly
- [ ] No new tables needed (use existing learning_insights if needed)

## Success Criteria
- Monitor can track basic statistics (review scores, timestamps)
- Monitor can detect idle state and suggest tasks
- Integration doesn't break existing inter-review flow